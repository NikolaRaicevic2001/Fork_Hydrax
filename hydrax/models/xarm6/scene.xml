<mujoco model="xarm6_stick_scene">
  <include file="xarm6.xml"/>

  <statistic center="0.3 0 0.3" extent="1.0"/>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
    <rgba haze="0.15 0.25 0.35 1"/>
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512" height="3072"/>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3"
      markrgb="0.8 0.8 0.8" width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5" reflectance="0.2"/>
  </asset>

  <worldbody>
    <light pos="0.3 0.3 1.5"/>
    <!-- Dropped 15cm below the robot's own base origin so the zero-config
         resting pose (stick tip at z=-0.0185, see verify_model.py output)
         doesn't start in contact with the floor. This is a bare
         verification scene, not a real table mount -- the arm can still
         reach well below this floor at other joint configurations (down
         to ~z=-0.49 at full range), so this offset is not a substitute for
         a real, joint-limit-aware workspace/table-height decision once
         this is composed into an actual task scene. -->
    <geom name="floor" pos="0 0 -0.15" size="0 0 .125" type="plane" material="groundplane"/>
  </worldbody>
</mujoco>
